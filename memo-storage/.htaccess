# Deny all direct web access — memo files here are only served through
# nge-employee-ob/download.php after an employee login check.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
